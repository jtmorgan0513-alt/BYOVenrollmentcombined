1. Wrap the wizard in a proper “card”

Right now the wizard content just sits on the page. Add a card shell so each step feels like a real form, not raw widgets.

Step 1 – Add these styles to your existing <style> block in main()

Find the big CSS chunk in main() (the one you already have for buttons, mobile, sidebar, etc.) and append this at the end of that <style> block:

/* Enrollment outer shell */
.byov-shell {
    max-width: 900px;
    margin: 0 auto;
    padding: 1.5rem 1.75rem 2rem;
    border-radius: 18px;
    background: linear-gradient(180deg, rgba(255,255,255,0.96), rgba(246,248,252,0.98));
    box-shadow: 0 18px 45px rgba(15,23,42,0.14);
    border: 1px solid rgba(15,23,42,0.06);
}

/* Step header bar */
.byov-step-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
    margin-bottom: 1.25rem;
}

.byov-step-title {
    font-size: 1.2rem;
    font-weight: 600;
    color: #0f172a;
}

.byov-step-sub {
    font-size: 0.9rem;
    color: #6b7280;
}

/* Section dividers inside the card */
.byov-section {
    padding: 0.75rem 0 0.5rem;
    margin-top: 0.35rem;
}

.byov-section-title {
    font-size: 0.95rem;
    font-weight: 600;
    color: #111827;
    display: flex;
    align-items: center;
    gap: 0.4rem;
    margin-bottom: 0.35rem;
}

.byov-section-title span.icon {
    font-size: 1.1rem;
}

/* Softer horizontal rule */
.byov-hr {
    border: none;
    border-top: 1px dashed rgba(148,163,184,0.7);
    margin: 0.75rem 0 0.5rem;
}

/* Progress labels */
.byov-progress-label {
    font-size: 0.8rem;
    font-weight: 500;
    text-align: center;
    padding: 0.25rem 0.5rem;
    border-radius: 999px;
    border: 1px solid transparent;
}

.byov-progress-label.completed {
    color: #16a34a;
    border-color: rgba(22,163,74,0.3);
    background: rgba(220,252,231,0.6);
}

.byov-progress-label.active {
    color: #0d6efd;
    border-color: rgba(13,110,253,0.4);
    background: rgba(219,234,254,0.7);
}

.byov-progress-label.pending {
    color: #6b7280;
    border-color: rgba(148,163,184,0.4);
    background: rgba(249,250,251,0.9);
}

/* Make warnings look more intentional */
.byov-tip {
    font-size: 0.86rem;
    color: #4b5563;
    background: rgba(219,234,254,0.65);
    border-radius: 10px;
    padding: 0.55rem 0.75rem;
    margin-bottom: 0.5rem;
}


No logic affected – this just defines CSS classes.

Step 2 – Use the shell and header in page_new_enrollment()

In page_new_enrollment(), you already render the progress bar and then call wizard_step_X(). Wrap the whole wizard content in the new shell:

Change this part:

st.markdown("---")

# Render appropriate step
if current_step == 1:
    wizard_step_1()
elif current_step == 2:
    wizard_step_2()
...


to:

st.markdown("<br>", unsafe_allow_html=True)

# Wrap the wizard in a card shell
st.markdown('<div class="byov-shell">', unsafe_allow_html=True)

# Optional: step-specific header text
step_titles = {
    1: ("Technician Information", "Tell us who you are and where you work."),
    2: ("Vehicle & Documents", "Add your vehicle details and upload required docs."),
    3: ("Policy & Signature", "Review the BYOV policy and sign electronically."),
    4: ("Review & Submit", "Double-check everything before you submit.")
}
title, subtitle = step_titles.get(current_step, ("BYOV Enrollment", ""))

st.markdown(
    f'''
    <div class="byov-step-header">
        <div>
            <div class="byov-step-title">{title}</div>
            <div class="byov-step-sub">{subtitle}</div>
        </div>
    </div>
    ''',
    unsafe_allow_html=True
)

# Render appropriate step
if current_step == 1:
    wizard_step_1()
elif current_step == 2:
    wizard_step_2()
elif current_step == 3:
    wizard_step_3()
elif current_step == 4:
    wizard_step_4()
else:
    st.error("Invalid wizard step")
    st.session_state.wizard_step = 1
    st.rerun()

st.markdown('</div>', unsafe_allow_html=True)


Again, this doesn’t touch any of the validation, DB, emails, or state; it only wraps what’s already there.

2. Make the progress bar look like real status pills

Right now your step labels are just Unicode circles and checks. You can keep the same logic and just apply the CSS classes you added.

In page_new_enrollment() where you build the progress indicator:

Find this loop:

for idx, (col, label) in enumerate(zip(progress_cols, step_labels), 1):
    with col:
        if idx < current_step:
            st.markdown(f"<div style='text-align: center; color: #28a745;'>✓ {label}</div>", unsafe_allow_html=True)
        elif idx == current_step:
            st.markdown(f"<div style='text-align: center; color: #007bff; font-weight: bold;'>● {label}</div>", unsafe_allow_html=True)
        else:
            st.markdown(f"<div style='text-align: center; color: #6c757d;'>○ {label}</div>", unsafe_allow_html=True)


Replace it with:

for idx, (col, label) in enumerate(zip(progress_cols, step_labels), 1):
    with col:
        if idx < current_step:
            cls = "byov-progress-label completed"
            symbol = "✓"
        elif idx == current_step:
            cls = "byov-progress-label active"
            symbol = "●"
        else:
            cls = "byov-progress-label pending"
            symbol = "○"
        
        st.markdown(
            f"<div class='{cls}'>{symbol} {label}</div>",
            unsafe_allow_html=True
        )


Same logic, same state, just better visuals.

3. Give each step nice section headings (no functional changes)

Inside your wizard steps you can sprinkle the new .byov-section and .byov-section-title to make things feel less like a blob of inputs.

Example in wizard_step_1() – right now you have:

st.subheader("Technician Information")
...
st.subheader("Industry Selection")
st.write("Select all industries that apply:")


You can keep the subheader or replace it with the new classes. Here’s a drop-in swap that won’t break anything:

st.markdown(
    """
    <div class="byov-section">
        <div class="byov-section-title">
            <span class="icon">👤</span>
            <span>Technician Information</span>
        </div>
    </div>
    """,
    unsafe_allow_html=True
)


Then before the industries section:

st.markdown(
    """
    <div class="byov-section">
        <div class="byov-section-title">
            <span class="icon">🏭</span>
            <span>Industry Selection</span>
        </div>
        <div class="byov-tip">
            Select all industries that apply to your current workload.
        </div>
    </div>
    """,
    unsafe_allow_html=True
)


Same idea for:

“Vehicle Information & Documents” → 🚗

“Registration” → 📋

“Insurance” → 🛡️

“BYOV Policy” → 📄

“Review Your Enrollment” → 🔍

You’re not changing any Streamlit widgets or state, only the text around them.

Net effect

Without touching:

Background handling

Theme colors

Validation / DB / email / dashboard sync

Admin mode / login

You get:

A proper card around the wizard

Nicer progress stepper

Cleaner section headers and spacing

The same behavior you already trust

If you want, I can turn this into a single “do this to byov_app.py” prompt you can paste into the Replit Claude agent like we did for the admin grid and keep it all automated.

You said:
I am going to need a copy and paste prompt for the replit agent to do this quickly I have a deadline to meet for today. 
ChatGPT said:
Thought for 59s

Here’s a single prompt you can copy-paste straight into the Replit Claude 4.5 agent. It only touches styling + the page_new_enrollment function and does NOT change any wizard logic, DB, emails, or background behavior.

Open the file byov_app.py.

Do NOT change any database logic, notification logic, upload logic, or wizard_step functions. Only make the two changes described below.

CHANGE 1 – Add nicer CSS for the enrollment shell and progress pills

In the main() function, find the big st.markdown(""" <style> ... </style> """, unsafe_allow_html=True) block that currently defines styles for .stApp, .main, .enrollment-form-container, .header-icon-button, @media (max-width: 768px), :root, .stButton>button, etc.

Inside that same <style> block, just BEFORE the closing </style>, append the following CSS:

    /* BYOV enrollment outer shell */
    .byov-shell {
        max-width: 900px;
        margin: 0 auto 1.5rem auto;
        padding: 1.5rem 1.75rem 2rem;
        border-radius: 18px;
        background: linear-gradient(180deg, rgba(255,255,255,0.96), rgba(246,248,252,0.98));
        box-shadow: 0 18px 45px rgba(15,23,42,0.14);
        border: 1px solid rgba(15,23,42,0.06);
    }

    .byov-step-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 0.75rem;
        margin-bottom: 1.25rem;
    }

    .byov-step-title {
        font-size: 1.2rem;
        font-weight: 600;
        color: #0f172a;
    }

    .byov-step-sub {
        font-size: 0.9rem;
        color: #6b7280;
    }

    .byov-progress-label {
        font-size: 0.8rem;
        font-weight: 500;
        text-align: center;
        padding: 0.25rem 0.5rem;
        border-radius: 999px;
        border: 1px solid transparent;
    }

    .byov-progress-label.completed {
        color: #16a34a;
        border-color: rgba(22,163,74,0.3);
        background: rgba(220,252,231,0.6);
    }

    .byov-progress-label.active {
        color: #0d6efd;
        border-color: rgba(13,110,253,0.4);
        background: rgba(219,234,254,0.7);
    }

    .byov-progress-label.pending {
        color: #6b7280;
        border-color: rgba(148,163,184,0.4);
        background: rgba(249,250,251,0.9);
    }


Leave all existing CSS in that block as-is and only append the above before </style>.

CHANGE 2 – Replace the page_new_enrollment function with a more polished wrapper

Now find the function:

def page_new_enrollment():
"""Main enrollment page with wizard navigation"""
...
# Progress indicator
current_step = st.session_state.wizard_step
# Progress bar
progress_cols = st.columns(4)
step_labels = [...]
for idx, (col, label) in enumerate(...):
...
st.markdown("---")
# Render appropriate step
if current_step == 1:
wizard_step_1()
elif current_step == 2:
wizard_step_2()
elif current_step == 3:
wizard_step_3()
elif current_step == 4:
wizard_step_4()
else:
...

Completely replace the ENTIRE body of page_new_enrollment (from the first line inside the function down to just before the next function definition) with the following implementation. Keep the same function name and docstring:

def page_new_enrollment():
"""Main enrollment page with wizard navigation"""

# Initialize wizard step if not exists
if 'wizard_step' not in st.session_state:
    st.session_state.wizard_step = 1

# Progress indicator
current_step = st.session_state.wizard_step

# Progress bar
progress_cols = st.columns(4)
step_labels = [
    "Technician Info",
    "Vehicle & Docs",
    "Policy & Signature",
    "Review & Submit"
]

for idx, (col, label) in enumerate(zip(progress_cols, step_labels), 1):
    with col:
        if idx < current_step:
            cls = "byov-progress-label completed"
            symbol = "✓"
        elif idx == current_step:
            cls = "byov-progress-label active"
            symbol = "●"
        else:
            cls = "byov-progress-label pending"
            symbol = "○"
        
        st.markdown(
            f"<div class='{cls}'>{symbol} {label}</div>",
            unsafe_allow_html=True
        )

# Small spacer instead of a hard rule
st.markdown("<br>", unsafe_allow_html=True)

# Wrap the wizard content in a visual shell/card
st.markdown('<div class="byov-shell">', unsafe_allow_html=True)

# Step-specific header text (purely visual, does not affect logic)
step_titles = {
    1: ("Technician Information", "Tell us who you are and where you work."),
    2: ("Vehicle & Documents", "Add your vehicle details and upload required docs."),
    3: ("Policy & Signature", "Review the BYOV policy and sign electronically."),
    4: ("Review & Submit", "Double-check everything before you submit.")
}
title, subtitle = step_titles.get(current_step, ("BYOV Enrollment", ""))

st.markdown(
    f"""
    <div class="byov-step-header">
        <div>
            <div class="byov-step-title">{title}</div>
            <div class="byov-step-sub">{subtitle}</div>
        </div>
    </div>
    """,
    unsafe_allow_html=True
)

# Render appropriate step content (logic unchanged)
if current_step == 1:
    wizard_step_1()
elif current_step == 2:
    wizard_step_2()
elif current_step == 3:
    wizard_step_3()
elif current_step == 4:
    wizard_step_4()
else:
    st.error("Invalid wizard step")
    st.session_state.wizard_step = 1
    st.rerun()

# Close shell
st.markdown("</div>", unsafe_allow_html=True)


Do NOT change anything else in byov_app.py. The wizard_step_1, wizard_step_2, wizard_step_3, wizard_step_4 functions must remain exactly as they are. All validation, uploads, database writes, notifications, and existing styling outside these two changes must remain untouched.